pyyaml>=6.0

# HTTP entrypoint (src/emotorad_ai/api.py) — what actually ships in the
# deployed container. Offline mode needs none of this; only serving it over
# HTTP does.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
websockets>=12.0

# Claude on Bedrock, once EMOTORAD_AI_MODE=bedrock is set (see api.py).
anthropic>=0.40

# Prompt-tuning playground (src/emotorad_ai/playground.py). Ships in the
# deployed container too — api.py reverse-proxies /playground to it so the
# internal team can reach it through the one port that's exposed, rather
# than it needing its own open port.
streamlit>=1.38

# Video attachments in the playground. Claude has no video block, so a customer's
# clip is sampled into still frames and sent as images; imageio-ffmpeg ships a
# static ffmpeg binary (~50MB) so no system package is needed. Drop both lines if
# you would rather the container stay small and videos be collected for a human
# to watch rather than assessed in chat — _extract_frames degrades to "could not
# be read" on its own, and the page keeps working.
imageio>=2.34
imageio-ffmpeg>=0.5

# Speech-to-text for a video's audio track: the customer's narration ("listen,
# it wheezes when I start it") is often the whole diagnosis, and frames throw
# the audio away. Runs locally and offline, so prompt tuning needs no extra
# credentials. Set EMOTORAD_WHISPER_MODEL=small (or medium) for noticeably
# better Hindi / Marathi / Hinglish, at the cost of a larger model download.
#
# For production prefer AWS Transcribe, which keeps this traffic inside the AWS
# boundary CLAUDE.md sets out; _transcribe_audio is the single seam to swap.
# Droppable like the imageio lines above: without it, videos still send frames
# and the narration is simply absent.
faster-whisper>=1.0

# Video evidence: src/emotorad_ai/video_summary.py hands a customer's clip to
# Gemini once at ingest and Claude reads the description. Absent GEMINI_API_KEY the frames path above runs instead.
google-genai>=1.0

# Startup config: src/emotorad_ai/config_store.py reads one Secrets Manager
# secret through the instance role and exports its fields as env vars. Local
# runs without EMOTORAD_AI_SECRET_ID never import boto3's network paths.
boto3>=1.34

# Image derivatives for guide media (scripts/upload_asset.py and the assets
# upload path): a 900px WebP made once at upload time, since S3 does not resize
# on request the way Cloudinary did. imageio already pulls Pillow in; pinning it
# here says the dependency is deliberate.
Pillow>=10

# Tracing: src/emotorad_ai/tracing.py sends each turn to Langfuse as a trace
# when LANGFUSE_PUBLIC_KEY and LANGFUSE_SECRET_KEY are set. Imported lazily, so
# a run without keys never loads it.
langfuse>=3.7,<4
